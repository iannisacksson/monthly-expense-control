const { Month } = require('./dist/models/month.model'); const { sequelize } = require('./dist/database/connection'); async function run() { try { const month = await Month.findOne({ where: { status: 'open', user_id: { [require('sequelize').Op.ne]: null } }, raw: true }); console.log(JSON.stringify(month)); } catch (e) { console.error(e); } finally { await sequelize.close(); } } run();

You are a senior backend engineer.

Your task is to generate the backend project setup using the documentation in /docs.

FIRST read and follow these documents:

docs/ai/backend-project-setup.md
docs/ai/backend-coding-rules.md
docs/architecture/backend-architecture.md

Follow the rules and architecture defined in those files.

Technology stack:

- Node.js
- TypeScript
- Express
- Sequelize
- PostgreSQL

Generate the backend setup step by step.

Step 1 — Generate package.json with required dependencies.

Step 2 — Generate tsconfig.json.

Step 3 — Create the backend folder structure as defined in backend-project-setup.md.

Step 4 — Generate the Express application file (src/app.ts).

Step 5 — Generate the HTTP server bootstrap (src/server.ts).

Step 6 — Generate the Sequelize database connection file (src/database/connection.ts).

Ensure the generated code follows the architecture and coding rules defined in the /docs directory.